---
phase: 01-landing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/editor/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor sees a clear product headline within the viewport on desktop and mobile"
    - "Visitor sees feature highlights (bento grid) below hero"
    - "Visitor can click a primary CTA and reach /signup"
    - "Visitor can click 'Sign In' and reach /login"
    - "Landing page renders without console errors and is responsive at 375px, 768px, 1280px"
  artifacts:
    - path: "apps/editor/app/page.tsx"
      provides: "Landing page UI with hero, bento, pricing, CTA, footer"
      contains: "href=\"/signup\""
  key_links:
    - from: "apps/editor/app/page.tsx"
      to: "/signup"
      via: "Link href"
      pattern: "href=\"/signup\""
    - from: "apps/editor/app/page.tsx"
      to: "/login"
      via: "Link href"
      pattern: "href=\"/login\""
---

<objective>
Audit and polish the existing landing page so all CTAs route to the canonical auth entry points and the page is verified responsive across breakpoints.

Purpose: Landing page is already built (hero, bento, pricing, CTA, footer). This plan does NOT rebuild — it standardizes CTA destinations to /signup and verifies LAND-01 + LAND-02 in the browser.
Output: Updated apps/editor/app/page.tsx with CTAs pointing to /signup, plus human-verified responsive behavior.
</objective>

<execution_context>
@C:/Users/faheem/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/faheem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing/01-RESEARCH.md

@apps/editor/app/page.tsx
@apps/editor/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize CTA destinations to /signup</name>
  <files>apps/editor/app/page.tsx</files>
  <action>
    Update all primary "Get Started" / "Sign Up" CTAs in apps/editor/app/page.tsx to point to /signup instead of /apply.

    Specific replacements (per grep — there are 5 instances of href="/apply"):
    - Line ~65/66: Top nav "Get Started Free" GlassButton — change href="/apply" to href="/signup"
    - Line ~93: Hero primary CTA GlassButton — change href="/apply" to href="/signup"
    - Line ~325: Use-cases section "Learn more" Link — change href="/apply" to href="/signup"
    - Line ~369: Pricing plan Links (free/pro tiers) — change href="/apply" to href="/signup"
    - Line ~387: Bottom CTA GlassButton — change href="/apply" to href="/signup"

    Leave href="/login" links untouched (Sign In is correctly wired).

    Do NOT change copy, layout, animations, or styling. Do NOT remove /apply route — it still exists for the beta funnel; we are simply standardizing public CTAs to /signup as the canonical auth entry per phase research.

    After edits, verify with: grep -n "href=\"/apply\"" apps/editor/app/page.tsx (should return zero results) and grep -n "href=\"/signup\"" apps/editor/app/page.tsx (should return 5+ results).
  </action>
  <verify>
    cd apps/editor && pnpm build
    grep -c "href=\"/signup\"" apps/editor/app/page.tsx  # >= 5
    grep -c "href=\"/apply\"" apps/editor/app/page.tsx   # == 0
  </verify>
  <done>
    All public-facing primary CTAs route to /signup. Sign In links route to /login. Build passes with no type errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of landing page responsiveness and CTA flows</name>
  <what-built>
    Landing page at / with all primary CTAs pointing to /signup and Sign In pointing to /login. Existing hero (animated 3D canvas), bento feature grid, how-it-works, use-cases, pricing, and footer all render.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` from apps/editor and open http://localhost:3000
    2. Confirm hero headline + tagline + primary CTA are visible above the fold on desktop (1280px viewport)
    3. Resize browser to 375px (mobile) — confirm hero, nav, CTAs remain usable; no horizontal scroll
    4. Resize to 768px (tablet) — confirm bento grid reflows correctly
    5. Click top-nav "Get Started Free" — should land on /signup
    6. Click hero primary CTA — should land on /signup
    7. Click any pricing plan CTA — should land on /signup
    8. Click bottom CTA section — should land on /signup
    9. Click top-nav "Sign In" — should land on /login
    10. Open browser devtools console — confirm zero errors (warnings about source maps are OK)
    11. Confirm feature highlights (bento) and pricing render with no broken images or layout shifts
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All 5+ CTA href="/apply" replaced with href="/signup"
- /login link untouched and functional
- Page renders responsive at 375 / 768 / 1280
- No console errors
- Build passes
</verification>

<success_criteria>
- LAND-01: Visitor sees clear headline, feature highlights, CTA above fold ✓
- LAND-02: Sign Up + Log In links work and route correctly ✓
- Page is responsive across all three breakpoints ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing/01-01-SUMMARY.md`
</output>
