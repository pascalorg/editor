{
  "name": "pascal",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/editor dev",
    "build": "bun run --cwd packages/editor build",
    "start": "bun run --cwd packages/editor start",
    "lint": "bun run --cwd packages/editor lint"
  }
}
