{
  "round": "seam-close round 2 (MERGE-GATE pixel re-read)",
  "pin": "0a3755303727b12b49e48be517be207356e68466",
  "branch": "feat/mep-glyph-decollision (2 commits on master 9c1490a; whole branch = paper-only: src/plans/plan-set.ts + its test, verified by git diff 9c1490a..0a37553)",
  "protocol": "Fix-check of blueprint-mep-glyph-seam.json (round-1, pin 88831fc) on the SAME round-8 composes per review/BLUEPRINTS.md. Pure buildPlanSet bun harness in a detached worktree at the exact pin (editor never opened): (A) demo-house-FL over HTTP (fc866f2f271b, 2108 members / 94 fixtures — live graph verified BYTE-EQUAL to the round-1 seam inputs, so every exhibit coordinate is directly comparable), (B) frost-mep-NY round-8 compose verbatim (saved round-1 members/fixtures 684/60 — engines untouched by the branch), (C) courtyard-NY round-8 compose verbatim (456/37). 38 sheets rasterized with Playwright page.setContent @2x + 17 zoom crops (3x-10x) of every glyph cluster; forensics independent of the drawing code: transform replica, SVG oriented-rect extraction with sourceId back-projection, bar-TIP point-to-rect clearances, crossings recount from walls x sleeved members, arrow-candidate replica, cite/marker text-rect separation math, round-1-vs-round-2 SVG diffs per sheet (non-MEP sheets byte-identical modulo harness title inputs). Evidence in /tmp/bp-seam2/ (svg + forensics.json + png + harness-rerun.log; harness = /tmp/bones-seam2/seam2-harness.ts).",
  "verdict": "REVISE",
  "suite": {
    "pass": 1018,
    "todo": 1,
    "fail": 0,
    "tests": 1019,
    "assertions": 31962,
    "files": 43,
    "tsc": "clean (bunx tsc --noEmit at the exact pin, detached worktree)"
  },
  "items": {
    "SWEEP-invariants": {
      "status": "PASS",
      "checklist": ["E1-E5", "S1-S11", "M1-M3", "P1-P5", "A1-A4"],
      "evidence": "bun test at 0a37553: 1018 pass / 1 todo / 0 fail — 1019 tests across 43 files, 31,962 expect() calls; tsc clean. +3 gates over round 1 (coaxial-pair arrow escape, cite census 1:1, courtyard tick invariants). Paper-only diff re-verified for the WHOLE branch (9c1490a..0a37553 touches only plan-set.ts + test); demo graph compute at the pin is byte-equal to the round-1 inputs. No gated CHECKLIST row regressed.",
      "note": "The FAILs below are on-paper classes on the round-8 composes that the branch's own gate composes do not reach — same pattern as round 1."
    },
    "FIX-1-marker-text (round-1 FAIL 1)": {
      "status": "PASS",
      "checklist": ["P4"],
      "evidence": "CLOSED on pixels + math. Courtyard exit: marker text now at center (939, 416) — the new widest-ring candidate — 18.2 px center / 4 px rect-gap from the exit SLEEVE cite at (927.4, 430) (round 1: 11.6 px, ghost-strokes text-over-text); viewed at 8x both bold lines print separate and fully legible, stacked above the equipment box. Water-heater box: 0 equipment-rect hits (the round-2 equipment registration works — round 1 ran the cite through the box fill); 0 pipe hits, on-sheet, bubbles clear. Frost marker clean at (657, 241.5): 12 px above its glyph, 0/0 hits, 25 px off the exit CO bubble, viewed at 5x. Demo marker: the equipment obstacle pushed glyph+text below the CU pad to (752.3, 542.1)/(752.3, 554.1) — clean white paper, association reads (viewed at 5x, aligned with the main's axis under the pad).",
      "note": "The round-1 recommendation (placed text rects + equipment rects as marker obstacles, no text-over-text at any tier) is implemented and holds on the round-8 compose, not just the gate compose."
    },
    "FIX-2-demo-arrow (round-1 FAIL 2)": {
      "status": "FAIL",
      "checklist": ["P4", "P3"],
      "evidence": "NOT fixed on the exhibit. The demo dwv-main flow arrow still prints at EXACTLY (752.3, 491.1) — SVG byte-identical to round 1 for every arrow on the sheet (the round-2 demo-MEP diff touches ONLY the sewer marker) — center 0.6 px from the suction elbow rect, glyph overlap −3.9 px, the same number rounds 8 and 1 measured. Viewed at 10x and 5x: the triangle interrupts BOTH pair hairlines where the line-set drops to the CU pad. Mechanism verified against the new code: run half = 14 px -> maxT = 8, so the along-run budget collapses to t=0; the perpendicular escape n in {±5, ±10} is bracketed — n=+5/+10 land INSIDE the suction horizontal leg (x .. 751.7), n=−5 lands between the rails (754.2 / 759.2), n=−10 reaches 7.6 px / 2.6 px from suction/liquid verticals — so tiers 1 (8.5) and 2 (4) reject every spot and tier 3 (bubbles-only, no pipe test) reprints the pre-fix coordinate. The pilot's synthetic coaxial-pair gate passes because its run has one fully clear side; the demo elbow has rails on BOTH sides plus the horizontal legs west.",
      "note": "Third consecutive round with this exact exhibit. The escape needs a larger n (±14 clears: 762.3+4 px puts the glyph edge ≥4 from the liquid rail at 759.2... verify against the real rects) or an along-run t finer than ±10 within maxT, or tier-3 should minimize overlap rather than take the first bubble-clean spot; pin the DEMO elbow geometry (short sloped main + two-sided coaxial pair at a corner), not just the one-sided compose."
    },
    "FIX-3-ticks": {
      "status": "FAIL",
      "checklist": ["P4", "P3"],
      "evidence": "Semantics restored, visibility destroyed — and the exit clearances still miss the branch's own bars. (a) SEMANTICS PASS: all 5 ticks (frost 2, courtyard 3) have bars genuinely crossing their OWN pipe (barsCrossOwn true, forensic replica) — the round-1 courtyard exit tick that floated 7.4 px off its pipe reading as ticking the LINE-SET is dead: it now sits ON the dwv-main at (927.4, 468), 8 px along-leg from its crossing, and at 10x the double-bar visibly crosses the main+pair bundle right at the east stemwall. Courtyard interior ticks + cites are the model read (viewed at 8x: bars across the corridor, cite 3 px clear above the bar tips). (b) NEW FAIL — cite-over-own-tick on VERTICAL legs: the round-2 own-tick exemption lets the perpendicular ring-14 cite candidate CONTAIN the tick (|dx|=14 < sleeveW/2=48 with dy=0), so BOTH frost cites print centered 14 px beside their tick at the same y — 'SLEEVE (P2603.4)' white halo erases the bars at (652, 278.5) and (657, 339.2); viewed at 8x/6x the ticks survive only as ghost notches between characters. The glyph the census was restored FOR is illegible at both frost crossings (round 8 had both ticks visible; round 1 had them visible with 1 cite). Horizontal legs are immune (the same ring lands dy=14 ABOVE the bars — courtyard proves the intent works). (c) EXIT-TICK CLEARANCES FAIL the branch's own bars on the round-8 compose: bar tips 2.9 px from the suction rail (gate + task bar: >= 4) and 8 px from the exit CO bubble center (gate bar: >= 12) — the east bar prints ~1.5 px inside the bubble ring (viewed at 10x: bar touches the ring; FOCUS-1c class). The least-crowded own-crossing fallback fired because nothing in the budget clears the CO+SO+DS+T+WH+CU cluster; the branch's courtyard gate compose (different heat-pump spot) never exercises this cluster — round 1 asked for exactly this pin.",
      "note": "Fixes are small: exclude cite candidates whose text rect intersects the tick GLYPH rect (±2.5 x ±6 + pad) — the along-leg ring spots (dy >= 14) already clear it; and either extend the tick's perpendicular budget / prefer bubble-side-clear spots at the exit, or draw ticks before the bubble pass. Pin BOTH: the frost vertical-leg cite geometry and the round-8 courtyard exit cluster."
    },
    "FIX-4-cite-census (round-1 FLAG)": {
      "status": "PASS",
      "checklist": ["P4"],
      "evidence": "Census restored 1:1:1 on both composes, judged against the CROSSINGS PRESENT: frost = 2 crossings (independent walls-x-sleeved-members recount: dwv-main x south stemwall + x wet-wall footing) = 2 ticks = 2 cites (round 1: 2/2/1); courtyard = 3 = 3 = 3 including the exit cite at (927.4, 430) that round 1 destroyed (round-8's cite was at (957, 354)-era coordinates on the pre-fix layout; the crossing pinned here is the same east-stemwall crossing). The pilot's 'frost gated 1/1' claim is honest for its GATE compose — that synthetic scene genuinely contains ONE sleeved crossing ('one sleeved crossing in this scene', verified in the test source), so 1 tick = 1 cite is the correct census THERE; the round-8 frost compose has 2 and prints 2/2 at the pin. Demo: 0 crossings, 0 ticks, 0 cites — correctly bare (FL shallow footings).",
      "note": "Numerically closed — but see FIX-3(b): both frost cites bought their census slot by printing on top of their own ticks. The census gate should grow a cite-rect-vs-tick-glyph separation assert or it will keep passing while the paper lies."
    },
    "FIX-5-legibility-1to75": {
      "status": "PASS",
      "checklist": ["P1", "P4", "P3", "M2", "M3"],
      "evidence": "All three MEP sheets read at print size with everything on them (viewed @2x full-sheet): frost — perimeter pair loop distinct from the slate tree and duct trunk, both SLEEVE cites + SEWER marker legible, converging trap arrows at the stack, 20 bubbles all clear; courtyard — the single-axis corridor band carries 3 visible ticks with cites at staggered heights, the exit cluster (CO+SO+DS+T+WH+CU+CO + marker + cite + tick + glyph) fully resolvable, marker/cite stack clean above the equipment box; demo — marker relocated below the CU pad reads clean, detour arrows and pair loop legible. The FAIL-class defects are sub-print-size (demo arrow overlap ~4 px; exit-tick bubble graze ~1.5 px) or glyph-level (frost tick halo-out) — none makes a sheet unreadable, all make it lie at zoom.",
      "note": "Legend box arithmetic unchanged from round 1 (rows inside the box on all three sheets, viewed); carried near-identical 'DWV drain / vent' vs 'supply / DWV pipe' slate swatches (pre-existing fallback-tint class)."
    },
    "REG-quick": {
      "status": "PASS",
      "checklist": ["P4", "P2", "P5", "M2", "S4", "C1"],
      "evidence": "Regression surface is provably tiny: per-sheet SVG diffs vs round 1 show non-MEP sheets byte-identical modulo the harness's own projectName/warnings/characteristics inputs (frost schedules row flow shifts only because the round-1 harness passed characteristics+warnings; content rows identical), and the demo MEP diff touches ONLY the sewer marker. Spot-walked anyway: slope rows — 'DWV SLOPE 1/4 IN/FT (1/8 AT 3 IN+)' + 'ARROWS POINT TO SEWER (P3005.3)' bold rows inside the legend box on all three MEP sheets; dashes — frost east elevation viewed at 3x: buried main falls DASHED through the dashed frost stemwalls below GRADE; pair colors — twin separation exactly 5.00 px on every full leg (frost 6/6, courtyard 2/2, demo 8 full legs; riser ticks pair at 2.69-5.22, the rolled-riser convention); flags — frost 'compressed to 4.91in/2.94in' x2 + the 29.5 m line-set advisory print verbatim; B17 — frost foundation legend intact (slab translucent row, 6-mil vapor retarder, 5/8in bolts 28 pcs, dowel circles 38 pcs, viewed). Arrow census stable-or-better: demo 3/3, frost 8/8 (coincident-candidate dedupe), courtyard 2/2 — round 1 drew only 1 (the exit dwv-main arrow was dropped in the crowded cluster; tier 3 recovered it).",
      "note": "The recovered courtyard exit arrow lands at (942.9, 458) with its apex ~1.8 px into the WH equipment box OUTLINE (tier 3 carries no pipe/equipment test) — see advisories."
    },
    "ADVISORIES": {
      "status": "FLAG",
      "evidence": "(1) Tier-3 arrow spots test bubbles only — the recovered courtyard exit arrow pokes the WH box outline by ~1.8 px (invisible at print size, visible at 5x); give tier 3 a minimize-overlap rule instead of first-fit. (2) Frost tick 1 rides 5 px off its pipe axis (t=12, n=5 diagonal escape) — bars cross the pipe by only ~3 px of span; moot while the cite covers it, but the barsCrossOwn threshold (lat <= 6+hw−2) accepts grazes that read weakly. (3) The frost exit cite's text rect touches the CO bubble edge at exactly 0 px (minBubbleEdge 0 — the placed[] test passes at |dy|=12 boundary); viewed acceptable, one px of drift breaks it. (4) Demo marker glyph now sits 24 px (0.63 m at 1:100) south of the physical exit with the CU pad between — association still reads (viewed), but the glyph no longer touches the pipe it marks. (5) Carried unchanged: frost 96.8 lf three-side line-set loop (route-cost probe queued), courtyard single-axis corridor band, near-identical slate legend swatches, pair-behind-bubbles convention.",
      "note": "None block beyond the scored items."
    }
  },
  "recommendations": [
    "Cites (FIX-3b): reject any cite candidate whose TEXT RECT intersects its own tick's glyph rect (tick half-extents ±2.5 along x ±6 across + 2 px pad) — the along-leg ring spots already clear it on vertical legs; keep the own-tick placed[] exemption for proximity, not containment. Pin on the round-8 frost compose: cite rect vs tick rect separation with census still 2/2.",
    "Demo arrow (FIX-2): the perpendicular escape needs to actually clear two-sided shadows — widen n to ±14/±16 (still visually attached at 1:100), or let tier 3 pick the minimum-overlap spot instead of first bubble-clean; gate on the REAL demo elbow geometry: short sloped main (half ~14 px) with pair verticals 2 px and 7 px off one side AND a pair horizontal leg on the other.",
    "Exit tick (FIX-3c): pin the round-8 courtyard exit cluster (auto condenser + F3 east exit) asserting bar tips >= 4 px off both rails AND >= 12 px bubbles with crossesOwn — the branch's courtyard gate compose (forced heat-pump at (12.5,0,5.5)) never fires the least-crowded fallback; alternatively draw ticks before the bubble pass so bubbles dodge them (closes the FOCUS-1c strike class for good).",
    "Give tier-3 arrow and marker fallbacks the equipment rects (they were registered for texts only in practice — the courtyard exit arrow apex clips the WH box outline).",
    "Board: both-directions line-set route probe stays worth its line — the frost loop is the collision surface for half of this file's history."
  ],
  "shipStamp": "REVISE — no merge stamp for feat/mep-glyph-decollision at 0a37553. Genuinely closed this round: the marker text class is DEAD on the round-8 composes (courtyard exit cite 18.2 px clear + 4 px rect gap, WH box clear, frost + demo markers clean — all viewed), cite census restored to crossings-present on every compose (frost 2/2/2 judged against the REAL compose, not the pilot's 1-crossing gate scene; courtyard 3/3/3 incl. the exit cite), all five ticks now genuinely cross their own pipe (the ticking-the-line-set read is dead), courtyard interior tick+cite pairs are the model read, arrow census 3/3 / 8/8 / 2/2 with a round-1 drop recovered, and the suite is 1018/1/0 + tsc clean with a provably paper-only diff. But the pixel re-read blocks on three concrete exhibits: (1) the demo flow arrow prints byte-identically to round 1 at (752.3, 491.1), −3.9 px INTO the suction elbow, third round running — the new escape's n<=10 budget is bracketed by rails on both sides and tier 3 reprints the pre-fix spot; (2) NEW class: on vertical legs the cite's own-tick exemption lets the ring-14 spot CONTAIN the tick — both frost sleeve ticks are haloed to ghost notches under their own 'SLEEVE (P2603.4)' text, so the census win erases the glyph it counts; (3) the courtyard exit tick's fallback lands bar tips 2.9 px from the suction rail and 1.5 px inside the CO bubble ring — under the branch's own >= 4 / >= 12 bars, on the compose its gates don't reach. All three fixes stay plan-set-only; the architecture (pipe+equipment+text rects as shared obstacles, tip-aware scoring, own-crossing hard invariant) is right — the search budgets and the cite/tick containment test are what's left."
}
