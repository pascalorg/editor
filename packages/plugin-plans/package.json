{
  "name": "@pascal-app/plugin-plans",
  "version": "0.1.0",
  "description": "Plans — a permit-ready construction set from the open scene. Ctrl+K → Generate plans: the scene goes to the Plans API (site + parcel + code checks + sheets) and the set opens inside the editor.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "peerDependencies": {
    "@pascal-app/core": ">=0.9.1 <1",
    "@pascal-app/editor": ">=0.9.1 <1",
    "lucide-react": "*",
    "react": "^18 || ^19",
    "react-dom": "^18 || ^19",
    "zustand": "^5"
  },
  "devDependencies": {
    "@pascal/typescript-config": "*",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
