/**
 * Formwork-assembly schema re-export. Canonical schema lives in
 * `@pascal-app/core` (same convention as wall/fence/door/window) — the
 * registry definition consumes it here so the rest of the bundle
 * imports a single canonical type.
 */
export { FormworkAssemblyNode } from '@pascal-app/core/schema'
