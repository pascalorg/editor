/**
 * The dedicated notes sheet (A0.1 general notes, multi-column, code-cited)
 * and the energy compliance sheet (EN1.0) — through the `general-notes` and
 * `energy` viewport kinds (providers/general-notes.ts, providers/energy.ts).
 *
 * OWNER: notes/energy workstream. Returns [] until it lands.
 */
import type { Plan, PlanSetContext } from './context'

export function notesPlans(_ctx: PlanSetContext): Plan[] {
  return []
}

export function energyPlans(_ctx: PlanSetContext): Plan[] {
  return []
}
