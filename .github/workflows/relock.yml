name: Relock

# Regenerates bun.lock on a real runner and pushes the result back to the
# branch it was dispatched on. The lockfile records the sha512 of each GitHub
# tarball, and only a machine that can reach the real api.github.com can
# compute those — a sandboxed environment cannot, so it delegates to this.
# Dispatch needs the file on the default branch, which this branch is not —
# so it fires on edits to itself instead. Its own bun.lock push cannot loop:
# it does not match the path filter, and GITHUB_TOKEN pushes start no runs.
on:
  push:
    branches: [claude/incele-nq4heb]
    paths: ['.github/workflows/relock.yml']

permissions:
  contents: write

jobs:
  relock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.0

      - name: Regenerate the lockfile
        run: bun install

      - name: Push it back
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add bun.lock
          if git diff --cached --quiet; then
            echo 'lockfile already in sync'
            exit 0
          fi
          git commit -m 'chore: regenerate bun.lock on a real runner'
          git push
